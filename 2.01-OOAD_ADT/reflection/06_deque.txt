ParentQueue написал правильно, отличие только в названии get_front - get_head.

Queue(ParentQueue) добавил алиас get для get_head(get_front), как было в эталоне обычной очереди. Это вполне логично,
ведь в стандартной очереди работа ведётся только с головой, и нет необходимости каждый раз добавлять _head(_front).

Deque(ParentQueue) правильно.
В эталоне size реализован как в ParentQueue, так и в Deque.
В моём же случае size реализован только в ParentQueue и работает для обоих типов очередей.

Предусловия и постусловия все верны.

Добавил алиасы enqueue, dequeue, first, last. Это должно быть удобнее, немного снижает нагрузку при чтении.

Написал реализацию с использованием двух массивов, один из которых перевёрнутый.
Мне казалось, что я уже делал подобное раньше, но не смог найти, поэтому написал с нуля.
Видимо, я только читал о таком способе.
