1.
is_first_row_smaller
// Сгруппировал определение булевой переменной и действия с ней.
// Раньше между определением и действиями был сторонний код.

2.
is_first_column_smaller
// Аналогично сгруппировал определение переменной и действия с ней.

3.
winner_position
// Переменная была определена заранее, но использовалась позже.
// Перенёс ближе к фактическому использованию.

4.
winner_percents
// Переменная была определена заранее, но использовалась позже.
// Перенёс ближе к фактическому использованию.

5.
for number in card_numbers: ...  - convert_to_binary()
// Была группа связанных команд, которая преобразовывала номера карт в бинарный формат.
// Создал их этих команд отдельную функцию.

6.
// Раньше была функция generate_permutations(...), которую вызывали напрямую и передавали параметры.
// Добавил функцию find_permutations(...), которая часть параметров рассчитывает самостоятельно и вызывает generate_permutations.
// Теперь нужно передавать меньше параметров.

7.
// Вынес обращение к стороннему сервису в отдельную функцию.

8.
// Вынес запись в файл в отдельную функцию.

9.
// Вынес сравнение версий файлов в отдельную функцию.

10.
// Вынес расчёт диагоналей из основного кода в отдельную функцию.

11.
// Перенёс счётчик, который определялся перед циклом, в заголовок цикла.

12.
// Перенёс определение переменной атрибута класса в функцию, т.к. она используется только внутри неё.

13.
// Превратил публичную переменную класса в приватную. Теперь она вызывается только другими методами класса.

14.
// Создал модуль и перенёс в него глобальную переменную.

15.
// Разделил основной большой метод на три коротких метода. Теперь основной метод вызывает вспомогательные.
