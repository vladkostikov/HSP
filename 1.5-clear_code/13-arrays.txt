1.
linked_files = []
...
linked_files.push("config/database.yml", "config/secrets.yml", "config/application.yml")
// В данном примере в массив добавляются пути к файлам и используются для последующего конфигурирования.
// Работа с массивом происходит без прямой индексации.

2.
inline_keyboard: = []
inline_keyboard.push([{ text: "Главное меню", callback_data: "main_menu" }])
inline_keyboard.push([{ text: "Корзина", callback_data: "cart" }]
// Массив используется для создания кнопок. Работа происходит без доступа по индексу.

3.
allowed_actions = [:index, :show]
// Массив необходим для перечисления допустимых действий. Доступ по индексу тоже не используется.

4.
queue.data = [None]
queue.add({ user_id: 19384, action: :check_balance })
// Для очереди использовался массив, с которым работали специальным образом.
// В данном случае лучше использовать очередь на основе АСД.

5.
content_type = ["image/png", "image/jpeg", "image/jpg"]
// Допустимые типы файлов записываются в массив и затем используются при валидации.
// Доступ по индексу не используется.
// Так как значения ожидаются уникальные, а порядок не важен, то лучше использовать set.
