1.
image_url = "https://api.telegram.org/file/bot#{Telegram.bot.token}/#{photo_file.file_path}"
// Формирование url для получения изображения.
// Т.к. формат ссылки не меняется, то такая жёсткая фиксация в коде вполне подходит.

2.
host = read_host_url()
// Здесь связывание происходит во время выполнения кода.
// Это необходимо т.к. у разных клиентов сервис может размещаться в разных регионах, на разных серверах.
// Например, клиенты из Европы используют "api.service.org", а из Америки "api.service.com".
// Так же сервис может быть размещён не на публичном сервере, а на собственном сервере клиента(self-hosted).

3.
EMAILS = ["help@school.ru", "support@school.ru"]
// Почты для отправки писем на технические адреса.
// Адреса жёстко зафиксированы в коде, думаю это плохой способ, но задачу фактически выполняет.
// В данном случае думаю лучше использовать хотя бы environment variable.
