7.1
undo_flag - is_last_action_undo

flag - is_every_citizen_walker

winner - is_winner_found

value_is_changed - is_value_changed

price_relevant - is_the_price_verified


7.2
done = False
// Перед запуском обновления ресурса задать переменную. Если ресурс успешно
// обновится, то в конце присвоить done = True.

success
// Если во время выполнения операции возникнет ошибка, то присвоить False.
// Если операция успешно завершится, то присвоить True.

error = False
// Изначально присвоить False. Если будет найдена ошибка, то присвоить True.


7.3
i - index
// Функция insert принимает индекс для вставки, но т.к. функция является
// довольно длинной и 7 раз обращается к этой переменной, то лучше
// назвать переменную нагляднее, а не просто i.


7.4
start_index/end_index
top_side/bottom_side, left_side/right_side
minimum/maximum


7.5
// Нашёл код, где вычисляется скорость с помощью "speed = speeds[i]", хотя
// за пару строк до этого есть переменная x, в которой уже записана эта скорость.
// Правильнее было бы сразу назвать x корректно и использовать эту переменную.

// Иногда в конце функции встречается присваивание значения переменной,
// а в следующей строке возврат этой же переменной.
// Возможно стоит обойтись без переменной и вернуть сразу значение.
